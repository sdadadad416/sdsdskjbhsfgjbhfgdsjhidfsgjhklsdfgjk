name: Free Windows Desktop with NoVNC + Cloudflare Tunnel

on: workflow_dispatch

jobs:
  setup-novnc-tunnel:
    runs-on: windows-latest
    steps:
    - name: Check out the repository
      uses: actions/checkout@v2

    - name: Download and Setup TightVNC Server
      run: |
        Write-Host "📥 Скачиваем TightVNC Server..."
        try {
            Invoke-WebRequest -Uri "https://www.tightvnc.com/download/2.8.85/tightvnc-2.8.85-gpl-setup-64bit.msi" -OutFile "$env:TEMP\tightvnc.msi"
            
            Write-Host "📦 Устанавливаем TightVNC Server..."
            $installArgs = @(
                '/i', "$env:TEMP\tightvnc.msi"
                '/quiet', '/norestart'
                'ADDLOCAL=Server'
                'SERVER_REGISTER_AS_SERVICE=1'
                'SERVER_ADD_FIREWALL_EXCEPTION=1'
                'SET_USEVNCAUTHENTICATION=1'
                'VALUE_OF_USEVNCAUTHENTICATION=1'
                'SET_PASSWORD=1'
                'VALUE_OF_PASSWORD=vnc123456'
                'SET_USECONTROLAUTHENTICATION=1'
                'VALUE_OF_USECONTROLAUTHENTICATION=1'
                'SET_CONTROLPASSWORD=1'
                'VALUE_OF_CONTROLPASSWORD=admin123456'
            )
            
            $process = Start-Process msiexec.exe -Wait -ArgumentList $installArgs -PassThru
            if ($process.ExitCode -eq 0) {
                Write-Host "✅ TightVNC Server установлен успешно!"
            } else {
                Write-Host "⚠️ TightVNC установлен с кодом: $($process.ExitCode)"
            }
        } catch {
            Write-Host "❌ Ошибка установки TightVNC: $($_.Exception.Message)"
        }
        Start-Sleep -Seconds 5

    - name: Download and Setup NoVNC
      run: |
        Write-Host "📥 Скачиваем NoVNC..."
        Invoke-WebRequest -Uri "https://github.com/novnc/noVNC/archive/refs/tags/v1.4.0.zip" -OutFile "$env:TEMP\novnc.zip"
        
        Write-Host "📦 Распаковываем NoVNC..."
        Expand-Archive -Path "$env:TEMP\novnc.zip" -DestinationPath "$env:USERPROFILE" -Force
        Rename-Item -Path "$env:USERPROFILE\noVNC-1.4.0" -NewName "novnc" -Force
        
        # Создаем index.html как копию vnc.html
        Write-Host "🔧 Создаем index.html..."
        if (Test-Path "$env:USERPROFILE\novnc\vnc.html") {
            Copy-Item -Path "$env:USERPROFILE\novnc\vnc.html" -Destination "$env:USERPROFILE\novnc\index.html" -Force
            Write-Host "✅ index.html создан"
        }
        
        # Создаем конфигурационный файл для websockify
        Write-Host "🔧 Создаем конфиг для websockify..."
        $tokenConfig = "default: localhost:5900"
        $tokenConfig | Out-File -FilePath "$env:USERPROFILE\novnc\vnc_tokens.txt" -Encoding ASCII
        Write-Host "✅ Конфиг создан"
        
        Write-Host "📥 Скачиваем websockify..."
        Invoke-WebRequest -Uri "https://github.com/novnc/websockify/archive/refs/tags/v0.11.0.zip" -OutFile "$env:TEMP\websockify.zip"
        Expand-Archive -Path "$env:TEMP\websockify.zip" -DestinationPath "$env:USERPROFILE" -Force
        Rename-Item -Path "$env:USERPROFILE\websockify-0.11.0" -NewName "websockify" -Force
        
        Write-Host "✅ NoVNC настроен!"

    - name: Setup Python Environment
      run: |
        Write-Host "🐍 Настраиваем Python окружение..."
        
        # Python уже установлен в GitHub Actions, просто обновляем pip
        python -m pip install --upgrade pip
        python -m pip install numpy
        
        Write-Host "✅ Python окружение готово!"

    - name: Download and Setup Cloudflare Tunnel
      run: |
        Write-Host "☁️ Скачиваем Cloudflare Tunnel (cloudflared)..."
        Invoke-WebRequest -Uri "https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-windows-amd64.exe" -OutFile "$env:USERPROFILE\cloudflared.exe"
        
        Write-Host "✅ Cloudflared готов!"

    - name: System Setup and User Configuration
      run: |
        Write-Host "⚙️ Настраиваем систему..."
        
        # Создаем пользователя для VNC
        Write-Host "👤 Настраиваем пользователя..."
        Set-LocalUser -Name "runneradmin" -Password (ConvertTo-SecureString -AsPlainText "Desktop2024!" -Force)
        
        # Настройка автологина
        Write-Host "🔐 Настраиваем автологин..."
        Set-ItemProperty -Path "HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Winlogon" -Name "AutoAdminLogon" -Value "1" -Type String
        Set-ItemProperty -Path "HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Winlogon" -Name "DefaultUserName" -Value "runneradmin" -Type String
        Set-ItemProperty -Path "HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Winlogon" -Name "DefaultPassword" -Value "Desktop2024!" -Type String
        
        # Отключаем экран блокировки
        Write-Host "🖥️ Отключаем блокировку экрана..."
        try {
            New-Item -Path "HKLM:\SOFTWARE\Policies\Microsoft\Windows" -Name "Personalization" -Force -ErrorAction SilentlyContinue
            Set-ItemProperty -Path "HKLM:\SOFTWARE\Policies\Microsoft\Windows\Personalization" -Name "NoLockScreen" -Value 1 -Type DWord -Force -ErrorAction SilentlyContinue
        } catch {
            Write-Host "⚠️ Не удалось отключить блокировку экрана: $($_.Exception.Message)"
        }
        
        # Настройка дисплея
        Write-Host "🖼️ Настраиваем разрешение экрана..."
        Add-Type -AssemblyName System.Windows.Forms
        $screen = [System.Windows.Forms.Screen]::PrimaryScreen
        Write-Host "Текущее разрешение: $($screen.Bounds.Width)x$($screen.Bounds.Height)"
        
        Write-Host "⌨️ Добавляем русскую раскладку..."
        try {
            $inputLanguageID = New-WinUserLanguageList -Language "ru-RU"
            $inputLanguageID += New-WinUserLanguageList -Language "en-US"
            Set-WinUserLanguageList -LanguageList $inputLanguageID -Force
        } catch {
            Write-Host "⚠️ Ошибка настройки языка: $($_.Exception.Message)"
        }
        
        Write-Host "✅ Система настроена!"

    - name: Start VNC Server
      run: |
        Write-Host "🖥️ Запускаем VNC Server..."
        
        # Проверяем установку TightVNC
        $tvnPath = "C:\Program Files\TightVNC\tvnserver.exe"
        if (-not (Test-Path $tvnPath)) {
            Write-Host "❌ TightVNC не найден по пути: $tvnPath"
            Write-Host "🔍 Ищем альтернативные пути..."
            $altPaths = @(
                "C:\Program Files (x86)\TightVNC\tvnserver.exe",
                "$env:ProgramFiles\TightVNC\tvnserver.exe",
                "${env:ProgramFiles(x86)}\TightVNC\tvnserver.exe"
            )
            foreach ($path in $altPaths) {
                if (Test-Path $path) {
                    $tvnPath = $path
                    Write-Host "✅ Найден TightVNC: $path"
                    break
                }
            }
        }
        
        if (Test-Path $tvnPath) {
            # Пробуем запустить сервис
            try {
                Start-Service -Name "tvnserver" -ErrorAction SilentlyContinue
                Start-Sleep -Seconds 5
            } catch {
                Write-Host "⚠️ Сервис не запустился: $($_.Exception.Message)"
            }
            
            # Альтернативный запуск если сервис не работает
            if ((Get-Service -Name "tvnserver" -ErrorAction SilentlyContinue).Status -ne "Running") {
                Write-Host "⚠️ Запускаем VNC вручную..."
                try {
                    Start-Process -FilePath $tvnPath -ArgumentList "-run" -WindowStyle Hidden
                    Start-Sleep -Seconds 10
                } catch {
                    Write-Host "❌ Ошибка запуска VNC: $($_.Exception.Message)"
                }
            }
            
            # Проверяем что VNC слушает порт 5900
            Start-Sleep -Seconds 5
            $vnc = Get-NetTCPConnection -LocalPort 5900 -ErrorAction SilentlyContinue
            if ($vnc) {
                Write-Host "✅ VNC Server запущен на порту 5900"
            } else {
                Write-Host "❌ VNC Server не слушает порт 5900"
                Write-Host "🔍 Проверяем все активные порты..."
                Get-NetTCPConnection | Where-Object {$_.LocalPort -like "59*"} | Format-Table LocalAddress,LocalPort,State
            }
        } else {
            Write-Host "❌ TightVNC не найден! Установка могла не завершиться."
        }

    - name: Start NoVNC Websockify
      run: |
        Write-Host "🌐 Запускаем NoVNC websockify..."
        
        # Обновляем PATH для Python
        $env:PATH = [System.Environment]::GetEnvironmentVariable("PATH","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("PATH","User")
        
        # Устанавливаем websockify через pip (более надежно)
        Write-Host "📦 Устанавливаем websockify через pip..."
        python -m pip install websockify
        
        # Проверяем что файлы на месте
        Write-Host "🔍 Проверяем файлы..."
        if (Test-Path "$env:USERPROFILE\novnc") {
            Write-Host "✅ NoVNC найден"
        } else {
            Write-Host "❌ NoVNC не найден"
        }
        
        # Проверяем что VNC работает перед запуском websockify
        Write-Host "� Проуверяем VNC перед запуском websockify..."
        $vncCheck = Get-NetTCPConnection -LocalPort 5900 -ErrorAction SilentlyContinue
        if ($vncCheck) {
            Write-Host "✅ VNC работает на порту 5900"
        } else {
            Write-Host "❌ VNC не работает на порту 5900!"
        }
        
        # Запускаем websockify в фоне с подробными логами
        Write-Host "🚀 Запускаем websockify..."
        $websockifyJob = Start-Job -ScriptBlock {
            param($novncPath)
            Write-Host "Запуск websockify с параметрами:"
            Write-Host "NoVNC path: $novncPath"
            Write-Host "Target: localhost:5900"
            Write-Host "Listen: 6080"
            
            # Запускаем websockify с правильными параметрами для NoVNC
            Write-Host "Запуск websockify для NoVNC..."
            python -m websockify --web="$novncPath" --verbose --target-config="$novncPath\vnc_tokens.txt" 6080 localhost:5900
        } -ArgumentList "$env:USERPROFILE\novnc"
        
        Start-Sleep -Seconds 20
        
        # Проверяем статус с подробной диагностикой
        $jobState = Get-Job -Id $websockifyJob.Id | Select-Object -ExpandProperty State
        Write-Host "📊 Статус websockify job: $jobState"
        
        if ($jobState -eq "Running") {
            Write-Host "✅ NoVNC websockify запущен!"
        } else {
            Write-Host "❌ Ошибка запуска websockify через pip:"
            $jobOutput = Receive-Job -Id $websockifyJob.Id
            $jobOutput | ForEach-Object { Write-Host "Job Output: $_" }
            
            # Пробуем альтернативный способ
            Write-Host "🔄 Пробуем альтернативный запуск..."
            Remove-Job -Id $websockifyJob.Id -Force -ErrorAction SilentlyContinue
            
            $websockifyJob = Start-Job -ScriptBlock {
                param($novncPath)
                Set-Location "$novncPath\utils"
                python websockify.py --web=.. --verbose 6080 localhost:5900
            } -ArgumentList "$env:USERPROFILE\novnc"
            
            Start-Sleep -Seconds 15
            $altJobState = Get-Job -Id $websockifyJob.Id | Select-Object -ExpandProperty State
            Write-Host "📊 Альтернативный статус: $altJobState"
            
            if ($altJobState -ne "Running") {
                Write-Host "❌ Альтернативный способ тоже не работает:"
                Receive-Job -Id $websockifyJob.Id | ForEach-Object { Write-Host "Alt Output: $_" }
            }
        }
        
        # Проверяем что websockify слушает порт 6080
        Start-Sleep -Seconds 5
        $websocket = Get-NetTCPConnection -LocalPort 6080 -ErrorAction SilentlyContinue
        if ($websocket) {
            Write-Host "✅ Websockify слушает порт 6080"
            
            # Тестируем HTTP подключение к websockify
            Write-Host "🔍 Тестируем HTTP подключение к websockify..."
            try {
                $response = Invoke-WebRequest -Uri "http://localhost:6080" -TimeoutSec 10 -ErrorAction Stop
                Write-Host "✅ HTTP ответ от websockify: $($response.StatusCode)"
                Write-Host "📄 Content-Type: $($response.Headers.'Content-Type')"
            } catch {
                Write-Host "❌ Ошибка HTTP подключения к websockify: $($_.Exception.Message)"
            }
            
            # Проверяем NoVNC файлы
            Write-Host "🔍 Проверяем NoVNC файлы..."
            $novncFiles = @(
                "$env:USERPROFILE\novnc\vnc.html",
                "$env:USERPROFILE\novnc\index.html",
                "$env:USERPROFILE\novnc\app\ui.js"
            )
            foreach ($file in $novncFiles) {
                if (Test-Path $file) {
                    Write-Host "✅ Найден: $file"
                } else {
                    Write-Host "❌ Отсутствует: $file"
                }
            }
            
            # Показываем содержимое папки NoVNC
            Write-Host "📁 Содержимое папки NoVNC:"
            Get-ChildItem "$env:USERPROFILE\novnc" | Select-Object Name, Length | Format-Table
            
        } else {
            Write-Host "❌ Websockify не слушает порт 6080"
            Write-Host "🔍 Проверяем активные порты:"
            Get-NetTCPConnection | Where-Object {$_.LocalPort -like "60*"} | Format-Table LocalAddress,LocalPort,State
            
            # Показываем ошибки websockify
            Write-Host "📋 Ошибки websockify:"
            Receive-Job -Id $websockifyJob.Id | ForEach-Object { Write-Host "Websockify: $_" }
        }

    - name: Start Cloudflare Tunnel and Keep Alive
      run: |
        Write-Host "☁️ Запускаем Cloudflare Tunnel..."
        
        # Запускаем cloudflared туннель в фоне
        Write-Host "🔗 Создаем туннель на trycloudflare.com..."
        $logFile = "$env:TEMP\cloudflared.log"
        
        # Запускаем в фоне с логированием
        $tunnelJob = Start-Job -ScriptBlock {
            param($cloudflaredPath, $logPath)
            & $cloudflaredPath tunnel --url http://localhost:6080 2>&1 | Tee-Object -FilePath $logPath
        } -ArgumentList "$env:USERPROFILE\cloudflared.exe", $logFile
        
        Start-Sleep -Seconds 30
        
        # Проверяем статус tunnel job
        $jobState = Get-Job -Id $tunnelJob.Id | Select-Object -ExpandProperty State
        if ($jobState -eq "Running") {
            Write-Host "✅ Cloudflare туннель успешно запущен!"
        } else {
            Write-Host "❌ Ошибка запуска Cloudflare Tunnel"
        }
        
        # Получаем URL туннеля (несколько попыток)
        $tunnelUrl = $null
        for ($i = 1; $i -le 8; $i++) {
            Write-Host "🔍 Попытка $i получить URL туннеля..."
            
            # Проверяем вывод job
            $tunnelOutput = Receive-Job -Id $tunnelJob.Id -Keep
            if ($tunnelOutput) {
                $tunnelOutput | ForEach-Object { 
                    Write-Host $_
                    if ($_ -match "https://[a-zA-Z0-9\-]+\.trycloudflare\.com") {
                        $tunnelUrl = $matches[0]
                        Write-Host "🎯 Найден URL в job: $tunnelUrl"
                    }
                }
            }
            
            # Проверяем лог файл
            if (Test-Path $logFile) {
                $logContent = Get-Content $logFile -ErrorAction SilentlyContinue
                if ($logContent) {
                    $logContent | ForEach-Object { 
                        if ($_ -match "https://[a-zA-Z0-9\-]+\.trycloudflare\.com") {
                            $tunnelUrl = $matches[0]
                            Write-Host "🎯 Найден URL в логе: $tunnelUrl"
                        }
                    }
                }
            }
            
            if ($tunnelUrl) { break }
            Start-Sleep -Seconds 15
        }
        
        if (-not $tunnelUrl) {
            Write-Host "⚠️ URL не найден, показываем все логи:"
            Write-Host "📋 Вывод Job:"
            Receive-Job -Id $tunnelJob.Id -Keep | ForEach-Object { Write-Host $_ }
            if (Test-Path $logFile) {
                Write-Host "📋 Содержимое лог файла:"
                Get-Content $logFile | ForEach-Object { Write-Host $_ }
            }
        }
        
        # Выводим информацию для подключения
        Write-Host ""
        Write-Host "=" * 70
        Write-Host "🌐 ИНФОРМАЦИЯ ДЛЯ ПОДКЛЮЧЕНИЯ К WINDOWS DESKTOP"
        Write-Host "=" * 70
        if ($tunnelUrl) {
            Write-Host "🔗 ПРЯМАЯ ССЫЛКА: $tunnelUrl"
            Write-Host "🎯 Скопируйте эту ссылку и откройте в браузере!"
        } else {
            Write-Host "🔗 Веб-интерфейс: Смотрите URL выше в логах Cloudflare"
            Write-Host "� оФормат: https://[random].trycloudflare.com"
        }
        Write-Host ""
        Write-Host "🔐 VNC Пароль: vnc123456"
        Write-Host "👤 Windows Пользователь: runneradmin"
        Write-Host "🔑 Windows Пароль: Desktop2024!"
        Write-Host ""
        Write-Host "💻 Откройте URL в браузере для доступа к рабочему столу"
        Write-Host "🎮 Полная поддержка клавиатуры и мыши"
        Write-Host "🌍 Работает в любом браузере (Chrome, Firefox, Safari, Edge)"
        Write-Host "📱 Даже на мобильных устройствах!"
        Write-Host "=" * 70
        Write-Host ""
        
        Write-Host "⏰ Поддерживаем работу туннеля..."
        Write-Host "🛑 Для остановки прервите выполнение workflow"
        Write-Host "📊 Туннель будет работать в течение ~6 часов"
        
        # Основной цикл поддержания работы (бесконечный)
        $startTime = Get-Date
        $lastCheck = Get-Date
        
        while ($true) {
            try {
                # Проверяем каждые 3 минуты
                if ((Get-Date) - $lastCheck -gt (New-TimeSpan -Minutes 3)) {
                    
                    # Проверяем Cloudflare tunnel
                    $currentTunnelState = Get-Job -Id $tunnelJob -ErrorAction SilentlyContinue | Select-Object -ExpandProperty State
                    if ($currentTunnelState -ne "Running") {
                        Write-Host "⚠️ Cloudflare tunnel остановлен, перезапускаем..."
                        Remove-Job -Id $tunnelJob -Force -ErrorAction SilentlyContinue
                        
                        # Перезапускаем туннель
                        $tunnelJob = Start-Job -ScriptBlock {
                            param($cloudflaredPath, $logPath)
                            & $cloudflaredPath tunnel --url http://localhost:6080 2>&1 | Tee-Object -FilePath $logPath
                        } -ArgumentList "$env:USERPROFILE\cloudflared.exe", $logFile
                        
                        Start-Sleep -Seconds 20
                        
                        # Показываем новый URL
                        $newOutput = Receive-Job -Id $tunnelJob -Keep
                        if ($newOutput) {
                            Write-Host "🔗 Новый вывод Cloudflare:"
                            $newOutput | ForEach-Object { 
                                Write-Host $_
                                if ($_ -match "https://[a-zA-Z0-9\-]+\.trycloudflare\.com") {
                                    Write-Host "🎯 Новый URL: $($matches[0])"
                                }
                            }
                        }
                    }
                    
                    # Проверяем VNC сервер
                    $vncPort = Get-NetTCPConnection -LocalPort 5900 -ErrorAction SilentlyContinue
                    if (-not $vncPort) {
                        Write-Host "⚠️ VNC Server остановлен, перезапускаем..."
                        Start-Process -FilePath "C:\Program Files\TightVNC\tvnserver.exe" -ArgumentList "-controlservice", "-start" -Wait
                    }
                    
                    # Проверяем websockify
                    $websocketPort = Get-NetTCPConnection -LocalPort 6080 -ErrorAction SilentlyContinue
                    if (-not $websocketPort) {
                        Write-Host "⚠️ Websockify остановлен на порту 6080, перезапускаем..."
                        
                        # Останавливаем старый job
                        Remove-Job -Id $websockifyJob -Force -ErrorAction SilentlyContinue
                        
                        # Перезапускаем websockify
                        $websockifyJob = Start-Job -ScriptBlock {
                            param($novncPath)
                            python -m websockify --web="$novncPath" --verbose 6080 localhost:5900
                        } -ArgumentList "$env:USERPROFILE\novnc"
                        
                        Start-Sleep -Seconds 15
                        Write-Host "🔄 Websockify перезапущен"
                    } else {
                        Write-Host "✅ Websockify работает на порту 6080"
                    }
                    
                    $lastCheck = Get-Date
                    Write-Host "✅ Проверка завершена - $(Get-Date -Format 'HH:mm:ss')"
                }
                
                Start-Sleep -Seconds 45
                
            } catch {
                Write-Host "❌ Ошибка в цикле мониторинга: $($_.Exception.Message)"
                Start-Sleep -Seconds 60
            }
        }
        
        # Этот код никогда не выполнится, так как цикл бесконечный
        Write-Host "⏰ Завершаем работу..."
        Remove-Job -Id $tunnelJob -Force -ErrorAction SilentlyContinue
